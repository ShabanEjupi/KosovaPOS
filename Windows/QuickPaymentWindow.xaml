<Window x:Class="KosovaPOS.Windows.QuickPaymentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Sa para dha klienti?" 
        Height="250" Width="400"
        WindowStartupLocation="CenterScreen"
        WindowStyle="ToolWindow"
        ResizeMode="NoResize"
        Background="#F5F5F5"
        ShowInTaskbar="False">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Total Amount Display -->
        <Border Grid.Row="0" Background="#2196F3" Padding="10" CornerRadius="5" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="TOTALI" 
                           FontSize="14" 
                           FontWeight="Bold" 
                           Foreground="White"
                           HorizontalAlignment="Center"/>
                <TextBlock x:Name="TotalAmountText" 
                           Text="0.00 €" 
                           FontSize="28" 
                           FontWeight="Bold" 
                           Foreground="White"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
        
        <!-- Cash Received Input -->
        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="Sa para dha klienti?" 
                       FontSize="16" 
                       FontWeight="Bold" 
                       Margin="0,0,0,5"/>
            <TextBox x:Name="CashReceivedTextBox" 
                     Height="50" 
                     FontSize="24" 
                     FontWeight="Bold"
                     TextAlignment="Center"
                     VerticalContentAlignment="Center"
                     Background="White"
                     BorderBrush="#4CAF50"
                     BorderThickness="3"
                     PreviewKeyDown="CashReceivedTextBox_KeyDown"/>
        </StackPanel>
        
        <!-- Change Amount Display -->
        <Border Grid.Row="2" Background="#FFEB3B" Padding="10" CornerRadius="5" Margin="0,0,0,10">
            <StackPanel>
                <TextBlock Text="KUSURI" 
                           FontSize="14" 
                           FontWeight="Bold" 
                           Foreground="#333"
                           HorizontalAlignment="Center"/>
                <TextBlock x:Name="ChangeAmountText" 
                           Text="0.00 €" 
                           FontSize="24" 
                           FontWeight="Bold" 
                           Foreground="#D32F2F"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
        
        <!-- Buttons -->
        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <Button x:Name="OkButton" 
                    Grid.Column="0"
                    Content="✓ OK (PRINTO)" 
                    Height="50"
                    Margin="0,0,5,0"
                    FontSize="16"
                    FontWeight="Bold"
                    Background="#4CAF50"
                    Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand"
                    Click="Ok_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#4CAF50"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#45A049"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            
            <Button x:Name="CancelButton" 
                    Grid.Column="1"
                    Content="✗ ANULO" 
                    Height="50"
                    Margin="5,0,0,0"
                    FontSize="16"
                    FontWeight="Bold"
                    Background="#F44336"
                    Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand"
                    Click="Cancel_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#F44336"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#DA190B"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
    </Grid>
</Window>
